# SecureMailScope Frontend Environment Variables

# Base URL for the Node.js API Gateway (NEVER Django!)
VITE_API_BASE_URL=http://localhost:3000

# Force mock mode (true / false). If false or unset, attempts to use live Node Gateway with graceful fallback.
VITE_USE_MOCK=false
